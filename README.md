# Zoomable DZI Image Viewer

This repository hosts a simple zoomable image viewer using **OpenSeadragon** and a **Deep Zoom Image (DZI)** created with **libvips**.

It is meant as a minimal template for publishing large images online for smooth zooming, navigation, archiving, and exhibitions.
